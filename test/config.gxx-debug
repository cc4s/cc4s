# python 3.4 + is necessary for the test framework to work
#
PYTHON ?= python3

# The ABSOLUTE path for the cc4s binary to test
#
CC4S_PATH ?= $(shell readlink -f ../build/$(CONFIG)/bin/Cc4s)

# The name of the output folder where the test is to be run
# this folder will be inside of the main test folder
#
OUTPUT_NAME ?= $(CONFIG)-$(shell md5sum $(CC4S_PATH) | cut -d ' ' -f1)
