CoulombVertexReader [
  (file "../../resources/CoulombVertex.bin")
] [
  CoulombVertex HoleEigenEnergies ParticleEigenEnergies
].

CoulombIntegralsFromVertex [
  CoulombVertex HoleEigenEnergies ParticleEigenEnergies
] [
  % Uncomment next line if you want PPPPIntegrals. Else slicing is used.
% PPPPCoulombIntegrals
  PHPHCoulombIntegrals
  PPHHCoulombIntegrals HHHHCoulombIntegrals
].

CcdEnergyFromCoulombIntegrals [
  % Uncomment next line if you want PPPPIntegrals. Else slicing is used.
% PPPPCoulombIntegrals
  % Comment next line if you want PPPPIntegrals. Else slicing is used.
  CoulombVertex
  PHPHCoulombIntegrals
  PPHHCoulombIntegrals HHHHCoulombIntegrals
  ParticleEigenEnergies HoleEigenEnergies
  % Sets maximum iterations. Default is 16.
  (maxIterations 16)
  % Uncomment next line to set the slice rank of PPPPIntegrals. Default is No.
% (sliceRank 40)
] [
  (CcdEnergy CcdEnergy) 
  (CcdDoublesAmplitudes CcdDoublesAmplitudes)
].


TensorWriter [(Data CcdEnergy)] [].
